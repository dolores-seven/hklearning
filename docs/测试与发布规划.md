# DSE 数学知识图谱 — 测试与发布规划

> 日期：2026-08-20

---

## 零、当前状态

### 文件清单

| 文件 | 大小 | 用途 | 上传 | 部署 |
|------|------|------|------|------|
| `dse-math-map.html` | 39KB | 渲染器（零依赖单文件） | ✅ | ✅ |
| `graph-data.js` | 222KB | 数据层 | ✅ | ✅ |
| `规划文档.md` | 16KB | 顶层设计文档 | ✅ | ❌ |
| `外部数据源调研报告.md` | 10KB | 调研结论 | ✅ | ❌ |
| `README.md` | 3.5KB | 使用说明 | ✅ | ✅ |
| `shots/*.png` | 6张 | 测试截图 | ✅ | ❌ |
| `_expand-*.js` / `add-cotested-final.js` | 4个 | 临时脚本 | ❌ | ❌ |

### 数据现状

```
38 课题（必修 26 + M2 12）
├─ 247 叶子（theorem 83 / corollary 94 / formula 70）
│   └─ pitfall 100% 覆盖
├─ 107 关系边（prereq 23 / derives 6 / related 12 / cotested 66）
├─ 55 母题（38/38 课题全覆盖）
└─ examFrequency 37/38（ms-02 缺失）
```

### 已发现的 2 个数据问题

1. **ms-02（三角学与极坐标）缺 examFrequency 和 examSection**
   - 原因：这个课题在之前 JSON 序列化时丢失了频率字段
   - 修复：补上 `examFrequency: 0, examSection: "P2"`（CAST 三角卷一从未考，只在卷二考）

2. **55 个母题全部缺少 `variation` 有效内容**
   - 字段名是 `variation`（不是规划文档里的 `variantDirections`）
   - 当前值可能是空字符串或无效
   - 需要逐一检查并确认是否真的缺内容

---

## 一、测试方案

### 1.1 数据层测试（Node 脚本，无需浏览器）

| 测试项 | 检查内容 | 通过标准 |
|--------|---------|--------|
| 语法校验 | `node -c graph-data.js` | 无报错 |
| 节点完整性 | 38 课题，每个有 id/name.zh/name.en/level/domain/grades/leaves | 全部通过 |
| 叶子完整性 | 247 叶子，每个有 id/name.zh/knowledgeType/formula/pitfall | 全部通过 |
| 叶子 knowledgeType | 只允许 formula/theorem/corollary 三种值 | 全部通过 |
| examFrequency | 38/38 课题都有，值在 0-13 之间 | 修复后全通过 |
| 边完整性 | 107 边，每条有 from/to/type，from/to 都是有效课题 id | 全部通过 |
| 边类型 | 只允许 prereq/derives/related/cotested | 全部通过 |
| 母题完整性 | 55 母题，每个有 id/titleZh/coreTopics/stem/solutionSkeleton/variation/appearances | 全部通过 |
| 母题引用 | coreTopics 和 relatedTopics 里的 id 都是有效课题 id | 全部通过 |
| 母题 variation | variation 非空 | 修复后全通过 |
| 母题 appearances | 每条有 year/paper/section/source | 全部通过 |
| 无重复 id | 课题 id、叶子 id、母题 id 无重复 | 全部通过 |
| 中英文名 | 每个课题和叶子都有 zh 和 en 两个名字 | 全部通过 |

### 1.2 渲染层测试（浏览器自动化）

| 测试项 | 操作 | 通过标准 |
|--------|------|--------|
| 页面加载 | 打开 HTML，检查 JS 错误 | 0 错误 |
| 节点渲染 | 统计 SVG circle 数量 | ≥ 38 |
| 边渲染 | 统计 SVG line 数量 | ≥ 107 |
| 点击详情 | 点击任一节点，检查详情面板出现 | 面板可见 |
| 母题区 | 检查详情面板内母题渲染 | 有母题标题+题干 |
| 下钻展开 | 双击节点，检查叶子节点出现 | 叶子 circle ≥ 1 |
| 叶子颜色 | 检查叶子节点的 fill 颜色 | formula=蓝/theorem=紫/corollary=琥珀 |
| 英文切换 | 点击语言切换 | 节点标签变为英文 |
| 年级过滤 | 勾选 S4 | 非 S4 节点淡出 |
| 模块切换 | 切换到 M2 | 只显示 M2 节点 |
| 边类型开关 | 关闭 cotested | 边数减少 |
| 频率高亮 | 勾选频率高亮 | 高频节点边框变色 |
| 搜索 | 输入关键词 | 非匹配节点淡出 |
| 焦点辐射 | 点击节点后 | 非关联节点淡出 |
| 图例完整 | 检查图例行数 | 12 行（4范畴+3知识类型+4边+1下钻） |
| 双击返回 | 双击空白处 | 叶子收起，回到课题视图 |

### 1.3 集成测试

| 测试项 | 操作 | 通过标准 |
|--------|------|--------|
| file:// 可用 | 双击 HTML 打开 | 数据加载成功，图正常渲染 |
| http:// 可用 | python -m http.server 打开 | 同上 |
| 移动端 | 浏览器 DevTools 切到 375px 宽 | 不溢出，核心功能可用 |

---

## 二、GitHub 上传

### 2.1 需要确认的信息

- **你的 GitHub 用户名？**
- **仓库名？**（建议 `dse-math-map` 或你已有的仓库名）
- **仓库是新建还是已有？**

### 2.2 上传文件清单

```
dse-math-map/
├── dse-math-map.html    # 渲染器
├── graph-data.js        # 数据层
├── README.md            # 使用说明（更新为英文，面向公开仓库）
├── docs/
│   ├── 规划文档.md
│   └── 外部数据源调研报告.md
└── shots/               # 测试截图（可选）
    ├── 01-default.png
    └── ...
```

### 2.3 不上传的文件

- `_expand-*.js`、`add-cotested-final.js` 等临时脚本
- `test-cdp.mjs`、`verify-ui.mjs` 等测试工具

### 2.4 操作步骤

1. 修复上述 2 个数据问题
2. 清理临时文件
3. `git init` + 首次 commit
4. 关联远程仓库 + push

---

## 三、网站部署

### 3.1 需要确认的信息

- **新加坡服务器的类型？**（VPS / 云函数 / 静态托管如 Cloudflare Pages / Netlify / Vercel）
- **有域名吗？**（如 `dse.example.com`）
- **怎么部署？**（SSH + nginx？还是 Git push 触发自动部署？）

### 3.2 部署方案（待你确认后细化）

因为是零依赖单文件 HTML + 一个 JS 数据文件，**最简单的部署方式是静态托管**：

- 把 `dse-math-map.html` + `graph-data.js` 传到服务器
- 配置 nginx/Apache 指向该目录
- 如果有域名，配 DNS 指向

不需要 Node.js、不需要数据库、不需要构建步骤。两个文件放上去就能用。

---

## 四、执行顺序

```
Step 1: 修复数据问题（ms-02 频率 + 母题 variation 检查）
Step 2: 清理临时文件
  ↓
Step 3: 运行数据层测试（Node 脚本）
  ↓
Step 4: 运行渲染层测试（浏览器自动化）
  ↓
Step 5: 修复测试发现的问题
  ↓
Step 6: 更新 README（面向公开）
  ↓
Step 7: GitHub 上传
  ↓
Step 8: 网站部署
```

---

## 五、需要你提供的信息

1. **GitHub 用户名 + 仓库名**（新建还是已有？）
2. **新加坡服务器类型 + 部署方式**（SSH + nginx？静态托管平台？）
3. **是否有域名？**

提供后我立刻按顺序执行。
